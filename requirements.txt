# Python deps for nucsegai_post_processing
# Matches conda env `nucsegai_post_processing` (Python 3.11).
#
# Install:
#   pip install -r requirements.txt
#
# Used by:
#   ilastik_* / latticea_test_data : h5py, numpy
#   region_classification          : numpy, pillow, matplotlib, scikit-learn, joblib
#   cellvit_inspection             : numpy, scipy, shapely

numpy>=1.20
h5py>=3.8
scipy>=1.7
shapely>=2.0
pillow>=9.0
matplotlib>=3.5
scikit-learn>=1.2
joblib>=1.2